name: DevSecOps Pipeline

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:

  # ---------------- 1. SonarQube Scan ----------------
  sonar_scan:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # FIX → Install unzip (required for sonar scanner)
      - name: Install unzip
        run: |
          sudo apt-get update -y
